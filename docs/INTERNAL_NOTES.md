# Internal Project Notes

## Identity & Publishing

- **GitHub Username**: `stancsz` (Used for repos, URLs, and Git clones)
- **NPM Username**: `stan-chen` (Used for scoped packages, e.g., `@stan-chen/simple-cli`)
- **Author Name**: Stan Chen

## Repository Structure

- `simple-cli`: Main repository (GitHub: `stancsz/simple-cli`)
- `TypeLLM`: LLM bridge library (GitHub: `stancsz/TypeLLM`, NPM: `@stan-chen/typellm`)

## Reminders

- Always use `stancsz` for GitHub related URLs.
- Always use `stan-chen` for NPM package scopes and publishing.
