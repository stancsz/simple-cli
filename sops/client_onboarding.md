## Goal
Onboard a new client efficiently.

## Prerequisites
- Client Name
- Contact Info

## Steps
1. Create a new directory for the client in `clients/`.
2. Initialize a git repository for the client project.
3. Send a welcome email template to the client contact.
