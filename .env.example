# ==============================================================================
# Simple-CLI Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE & ESSENTIAL KEYS
# ------------------------------------------------------------------------------
# Primary key for the core engine (src/llm.ts) and default fallback.
# Required for: General usage, CrewAI fallback.
OPENAI_API_KEY=sk-...

# Crucial for specialized "DeepSeek" sub-agents.
# Required for: deepseek_claude, deepseek_aider, deepseek_opencode, deepseek_crewai.
# This key is often mapped internally to power other tools via DeepSeek's API.
DEEPSEEK_API_KEY=sk-...

# ------------------------------------------------------------------------------
# 2. AGENT-SPECIFIC KEYS
# ------------------------------------------------------------------------------
# Required for the Jules agent/MCP server (src/mcp_servers/jules/).
# Enables: Delegation of full-stack tasks and PR management via Jules API.
JULES_API_KEY=...

# Required for GitHub operations (creating PRs, listing issues).
# Used by: `gh` CLI and Jules agent.
# The `gh` CLI checks `GH_TOKEN` first, then `GITHUB_TOKEN`. Providing either one is sufficient.
# Alternative: Run `gh auth login` interactively.
GH_TOKEN=ghp_...
# GITHUB_TOKEN=ghp_...

# ------------------------------------------------------------------------------
# 3. OPTIONAL / PROVIDER-SPECIFIC KEYS
# ------------------------------------------------------------------------------
# Required if explicitly configuring the core engine to use Anthropic models directly.
# (Note: deepseek_claude uses DEEPSEEK_API_KEY by default).
ANTHROPIC_API_KEY=sk-ant-...

# Required if configuring the core engine to use Google Gemini models.
# The application checks both keys; providing either one is sufficient.
GOOGLE_API_KEY=AIzaSy...
# GEMINI_API_KEY=AIzaSy...

# ------------------------------------------------------------------------------
# 4. CONFIGURATION & DEFAULTS
# ------------------------------------------------------------------------------
# Default Model Configuration for the core engine.
# Format: provider:model
# Examples:
#   openai:gpt-4o
#   deepseek:deepseek-chat
#   anthropic:claude-3-5-sonnet-latest
MODEL=deepseek:deepseek-reasoner
# Options:
# deepseek:deepseek-reasoner (uses Anthropic SDK compatibility - "Claude + DeepSeek")
# deepseek-openai:deepseek-reasoner (uses OpenAI SDK compatibility - "Codex + DeepSeek")
